{
  "name": "weather-app-mern-root",
  "version": "1.0.0",
  "private": true,
  "description": "Orchestrator for MERN weather app",
  "scripts": {
    "install-all": "npm install && npm install --prefix api && npm install --prefix client",
    "dev": "docker-compose up --build",
    "down": "docker-compose down",
    "vercel-build": "npm install --prefix api && npm install --prefix client && npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
